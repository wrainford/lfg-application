<%- include("./partials/header") %>

<p><% user.favoriteGames.forEach(function(i) { %></p>
    <%= i.title %>
    <% }) %>

<form action="/addfavorites/<%= user.id %>" method="get">
    <input type="submit" value="Edit List" class="btn btn-default">
</form>

<%- include("./partials/footer") %>